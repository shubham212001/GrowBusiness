<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sharma.growbusiness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrowBusiness">
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".PurchaseActivity" />
        <activity android:name=".Add_PurchaseActivity" />
        <activity android:name=".temp_item_activity" />
        <activity android:name=".AddSalesActivity" />
        <activity android:name=".navigation" />
        <activity android:name=".dashBoard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>